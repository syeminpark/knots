const SidebarLeft = (props) => {
    const { panels, setPanels } = props

    return (
        < button className="sidebarLeft"  >
            <i className="sidebarLeft-title">Journal</i>
        </button >
    )
}

export default SidebarLeft